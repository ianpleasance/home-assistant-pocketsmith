views:
  - title: PocketSmith Simple
    path: pocketsmith-simple
    icon: mdi:wallet-outline
    badges: []
    cards:
      - type: markdown
        title: 💳 NatWest Primary Account
        content: >
          {% set account =
          'sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_account_4546656' %}

          {% set transactions =
          state_attr('sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_transactions_4546656',
          'transactions') %}

          {% set symbol = state_attr(account, 'currency_symbol') or
          state_attr(account, 'currency') %}


          **Balance: {{ symbol }}{{ states(account) }}**


          {% if transactions %}

          **Recent Transactions:**

          {% for t in transactions[:5] -%}

          • {{ t.date }} - {{ t.payee[:40] }} - {{ symbol }}{{
          "%.2f"|format(t.amount) }}

          {% endfor %}

          {% else %}

          No recent transactions

          {% endif %}
      - type: markdown
        title: 💳 Monzo Personal Account
        content: >
          {% set account =
          'sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_account_4574043' %}

          {% set transactions =
          state_attr('sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_transactions_4574043',
          'transactions') %}

          {% set symbol = state_attr(account, 'currency_symbol') or
          state_attr(account, 'currency') %}


          **Balance: {{ symbol }}{{ states(account) }}**


          {% if transactions %}

          **Recent Transactions:**

          {% for t in transactions[:5] -%}

          • {{ t.date }} - {{ t.payee[:40] }} - {{ symbol }}{{
          "%.2f"|format(t.amount) }}

          {% endfor %}

          {% else %}

          No recent transactions

          {% endif %}
      - type: markdown
        title: 🐷 Monzo Savings
        content: >
          {% set account =
          'sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_account_4574040' %}

          {% set transactions =
          state_attr('sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_transactions_4574040',
          'transactions') %}

          {% set symbol = state_attr(account, 'currency_symbol') or
          state_attr(account, 'currency') %}


          **Balance: {{ symbol }}{{ states(account) }}**


          {% if transactions %}

          **Recent Transactions:**

          {% for t in transactions[:5] -%}

          • {{ t.date }} - {{ t.payee[:40] }} - {{ symbol }}{{
          "%.2f"|format(t.amount) }}

          {% endfor %}

          {% else %}

          No recent transactions

          {% endif %}
      - type: markdown
        title: 💳 Revolut (GBP)
        content: >
          {% set account =
          'sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_account_4571421' %}

          {% set transactions =
          state_attr('sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_transactions_4571421',
          'transactions') %}

          {% set symbol = state_attr(account, 'currency_symbol') or
          state_attr(account, 'currency') %}


          **Balance: {{ symbol }}{{ states(account) }}**


          {% if transactions %}

          **Recent Transactions:**

          {% for t in transactions[:5] -%}

          • {{ t.date }} - {{ t.payee[:40] }} - {{ symbol }}{{
          "%.2f"|format(t.amount) }}

          {% endfor %}

          {% else %}

          No recent transactions

          {% endif %}
      - type: markdown
        title: 💶 Revolut (EUR)
        content: >
          {% set account =
          'sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_account_4571418' %}

          {% set transactions =
          state_attr('sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_transactions_4571418',
          'transactions') %}

          {% set symbol = state_attr(account, 'currency_symbol') or
          state_attr(account, 'currency') %}


          **Balance: {{ symbol }}{{ states(account) }}**


          {% if transactions %}

          **Recent Transactions:**

          {% for t in transactions[:5] -%}

          • {{ t.date }} - {{ t.payee[:40] }} - {{ symbol }}{{
          "%.2f"|format(t.amount) }}

          {% endfor %}

          {% else %}

          No recent transactions

          {% endif %}
      - type: markdown
        title: 💵 Revolut (USD)
        content: >
          {% set account =
          'sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_account_4571424' %}

          {% set transactions =
          state_attr('sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_transactions_4571424',
          'transactions') %}

          {% set symbol = state_attr(account, 'currency_symbol') or
          state_attr(account, 'currency') %}


          **Balance: {{ symbol }}{{ states(account) }}**


          {% if transactions %}

          **Recent Transactions:**

          {% for t in transactions[:5] -%}

          • {{ t.date }} - {{ t.payee[:40] }} - {{ symbol }}{{
          "%.2f"|format(t.amount) }}

          {% endfor %}

          {% else %}

          No recent transactions

          {% endif %}
      - type: markdown
        title: 💳 American Express
        content: >
          {% set account =
          'sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_account_4546611' %}

          {% set transactions =
          state_attr('sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_transactions_4546611',
          'transactions') %}

          {% set symbol = state_attr(account, 'currency_symbol') or
          state_attr(account, 'currency') %}


          **Balance: {{ symbol }}{{ states(account) }}**


          {% if transactions %}

          **Recent Transactions:**

          {% for t in transactions[:5] -%}

          • {{ t.date }} - {{ t.payee[:40] }} - {{ symbol }}{{
          "%.2f"|format(t.amount) }}

          {% endfor %}

          {% else %}

          No recent transactions

          {% endif %}
      - type: markdown
        title: 💳 NatWest Mastercard
        content: >
          {% set account =
          'sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_account_4546653' %}

          {% set transactions =
          state_attr('sensor.pocketsmith_01kez6197kh7b8ezepwe2tnbcp_transactions_4546653',
          'transactions') %}

          {% set symbol = state_attr(account, 'currency_symbol') or
          state_attr(account, 'currency') %}


          **Balance: {{ symbol }}{{ states(account) }}**


          {% if transactions %}

          **Recent Transactions:**

          {% for t in transactions[:5] -%}

          • {{ t.date }} - {{ t.payee[:40] }} - {{ symbol }}{{
          "%.2f"|format(t.amount) }}

          {% endfor %}

          {% else %}

          No recent transactions

          {% endif %}

